{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://journium.app/schemas/journium-insight-tracker.schema.json",
    "title": "Journium InsightTracker",
    "description": "Root schema for Journium InsightTracker. All trackers must use apiVersion 'journium.app/v0Beta'.",
    "type": "object",
    "required": [
        "apiVersion"
    ],
    "properties": {
        "apiVersion": {
            "type": "string",
            "description": "Schema version discriminant. Must be exactly 'journium.app/v0Beta'."
        }
    },
    "oneOf": [
        {
            "properties": {
                "apiVersion": {
                    "const": "journium.app/v0Beta"
                }
            },
            "required": [
                "apiVersion"
            ],
            "$ref": "https://journium.app/schemas/journium-insight-tracker.v0Beta.schema.json"
        }
    ]
}
